---
description: Loyihani ishga tushirish va boshqarish
---
# Termiziy AI - Ishga tushirish

// turbo-all

## Dev serverni ishga tushirish
```cmd
cmd /c "npm run dev"
```

## Build qilish
```cmd
cmd /c "npm run build"
```

## TypeScript tekshirish
```cmd
cmd /c "npx tsc --noEmit"
```

## ESLint tekshirish
```cmd
cmd /c "npx eslint src/app/page.tsx"
```

## Port 3000 ni bo'shatish (agar band bo'lsa)
```cmd
taskkill /F /IM node.exe
```
